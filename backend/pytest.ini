[pytest]
markers =
    config_only: test only exercises how configuration is reported; it names a
        real backend but never builds a store, so the isolation guard in
        tests/conftest.py skips its NOTE_BACKEND check.
